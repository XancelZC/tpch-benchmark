# MatrixBench AI 使用说明

## 1. 使用了哪些 AI 工具

本项目使用 Hermes 和 ClaudeCode辅助完成。

## 2. 关键 Prompt 示例

**Prompt 1：需求拆解与方案设计**

```
你是一名资深数据库性能测试工程师，负责为客户做数据库选型 PoC。

请为我设计一个多数据库查询性能对比工具的技术方案，要求：
- 覆盖 FDE 客户场景：PoC 验证、迁移评估、竞品对比
- 支持 PostgreSQL / MySQL / YMatrix / Greenplum 四库
- 基于 TPC-H 22 条分析型查询

约束：
1. 数据必须可复现——说明数据生成方式和版本锁定手段
2. 统计口径必须稳健——明确为什么不能用算术平均 speedup 做唯一结论
3. 结果必须可验证——说明如何证明四个库返回的业务结果一致
4. 边界要诚实——列出你认为当前方案无法保证公平的变量

输出：Markdown 结构，包含模块划分、每个模块的设计理由、被放弃的备选方案。先给一句话骨架，再展开细节。
```

**Prompt 2：数据生成方案纠偏**

```
你是一名数据工程专家。请审查以下 TPC-H 测试数据方案的可信度：

背景：当前数据由自写随机生成器产生，行数为 region=5, nation=25, supplier=1000, customer=15000, part=20000, partsupp=80000, orders=150000, lineitem=600572。

请回答：
1. 这些行数与 TPC-H SF=0.1 标准基数是否一致？
2. 自写生成器能否保证 partsupp 与 lineitem 的 (part, supplier) 关系完整？
3. 如果不可信，给出可复现的替代方案，并设计验证手段（行数校验、关系校验、谓词命中校验）。

要求：先读代码和数据实际验证，再下结论，不要凭经验猜测。
```

**Prompt 3：部署排障**

```
你是一名熟悉各种专业数据库的数据库运维工程师。

环境：Apple Silicon Mac + OrbStack + Rosetta，运行 matrixdb/matrixdb-community:v5.2.1 社区版容器。

现象：执行 mxsetup 部署集群，到 launch_matrixdb 阶段报 DeadlineExceeded，随后工具自动 revert，清空所有已初始化数据，反复重试无效。

请分析：
1. 超时发生在 launch 阶段的可能根因
2. 为什么 mxsetup 是原子操作、失败即回滚——这对排障有什么影响
3. 给出绕过自动回滚的分步部署方案，并说明每步验证点

要求：给出可执行的命令序列，注明失败时如何保留现场而不是清空。
```

**Prompt 4：结果可信度复核**

```
你是一名统计方法论专家，请审查一个数据库性能对比报告的指标口径。

背景：4 个数据库跑 TPC-H 22 条查询，每条查询 5 轮。当前结论用"逐查询 speedup 的算术平均"作为总体性能指标。已知 Q8、Q21 等查询的 speedup 存在极端值（如某查询 YMatrix 比 MySQL 快 16 倍）。

请评估：
1. 算术平均在该场景下的偏差来源
2. 替代指标：中位数、几何均值、总耗时比各自的适用性和局限
3. 给出推荐的多指标组合，并说明每个指标回答什么问题
4. 如何用现有 raw 数据计算并验证

输出：指标对比表 + 推荐口径 + 计算验证方法。
```

## 3. AI 帮助完成了哪些部分

- 需求拆解和 FDE 场景建模（把题目转化为可运行的模块划分）；
- Python 连接器、执行器、统计和报告代码；
- YAML 配置和 SQL 目录整理；
- Docker/YMatrix/Greenplum 部署排障；
- 测试脚本、数据校验脚本和文档初稿；
- 数据生成链路和结果校验方案设计。

## 4. AI 生成内容中出现过哪些问题

### 自制数据被误当成 TPC-H 数据

初版随机生成器没有保持 partsupp 与 lineitem 的关系，也没有保证查询谓词命中。通过扫描 60 万行和检查表关系后发现问题，最终切换到固定 dbgen 数据，并增加 manifest 和完整性校验。

### 公开产品可用性判断错误

早期曾根据产品定位猜测 YMatrix 没有可用社区镜像。随后直接搜索和拉取官方社区镜像，确认并实际部署了 YMatrix 5.2.1。

### 环境架构不一致

初版将 YMatrix amd64+Rosetta 与其他 ARM 原生数据库直接比较。发现架构路径不一致后，增加混合环境观察，并最终构建四库统一 amd64 的受控环境。

### 索引缺失造成假异常

重建容器时只恢复数据，遗漏索引和统计信息，导致查询出现数百秒异常。通过 EXPLAIN 定位后，把逻辑主键、访问路径索引和 ANALYZE 纳入正式初始化脚本。

### 平均加速比被离群值放大

初版只使用逐查询 speedup 算术平均。复核 Q8/Q21 等离群查询后，改为同时展示总耗时比、中位数、几何均值和胜负数。

### MPP 初始化脚本多层引号失败

初版通过多层 `su -c` 和 `psql -c` 拼接 COPY 命令，在 YMatrix/Greenplum 中出现引号错误。最终改为固定 SQL 文件、绝对 psql 路径和非登录 runuser 环境。

## 5. 你如何验证和修正

### 验证方式

AI 不能替代以下验证：

- 数据行数和关系完整性（`validate_data.py` 6 项校验）；
- 四库实际架构和资源限制（统一 amd64 + 4 CPU/3 GiB）；
- SQL 执行成功与结果正确的区别（结果 hash 跨库比对）；
- 超时是否真正取消数据库端查询（`SELECT SLEEP(2)` 实测）；
- 指标是否被极端值误导（中位数/几何均值/总耗时比交叉验证）；
- README 是否能从空环境复现（一键 bootstrap 实测）。

最终项目包含 23 个自动化测试，并通过 Ruff、Python 编译、Shell 语法和真实四库执行验证。

### 修正流程

每个问题案例都遵循"发现问题 → 定位根因 → 修正 → 重新验证"的闭环，例如：

- 发现自制数据关系不完整 → 换 dbgen + manifest 校验 → 四库行数/谓词复核通过；
- 发现索引缺失 → 补齐索引 SQL 纳入初始化脚本 → Q8 从数百秒恢复到百毫秒级；
- 发现平均加速比误导 → 改多指标口径 → 重新计算并复核报告。

## 6. 如果不使用 AI，预计需要多久完成

按单人独立开发估算：

| 环节                                              |  不使用 AI  |
| ------------------------------------------------- | :---------: |
| 需求拆解与方案设计                                |    1 天     |
| 数据生成与校验链路（dbgen + manifest + 6 项校验） |    2 天     |
| 四库连接器与执行引擎（含超时/预热/随机顺序）      |    1 天     |
| 统计与报告（多指标 + CSV/Markdown）               |    1 天     |
| YMatrix/Greenplum 部署排障（Rosetta 等）          |    1 天     |
| 测试、文档与交付整理                              |    2 天     |
| **合计**                                          | **约 8 天** |
